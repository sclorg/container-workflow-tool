[tox]
skipsdist = True
envlist = py311,py312,py313,py314

[testenv]
commands = python3 -m pytest --color=yes -v --showlocals tests/
deps =
    pytest
    pytest-cov
    flexmock
    GitPython
    requests
    PyYAML

[flake8]
max-line-length = 120
